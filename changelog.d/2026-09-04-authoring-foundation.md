# Authoring foundation

* added ordered, backed-up project format migrations for chapter, board, and scene identity
* added immutable inline scene IDs and removed those markers from compile and reading output
* added preview-first scene move, split, and merge commands with automatic checkpoints
* added durable restore transaction journals with automatic interruption recovery
* added editable chapter and scene text plus local LanguageTool findings to the TUI
* added read-only rewrite, outline, focused review, metadata, brainstorming, and project-query AI commands
* defined the local-first Word round-trip design under `specs/004-word-round-trip/`
